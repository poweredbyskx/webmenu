# План действий — сайт КАКАО

---

## Прямо сейчас

- [ ] Смёржить ветку `claude/project-analysis-improvements-8c09d2` в `main`
- [ ] Задеплоить на сервер (`git pull` на сервере + `systemctl restart gunicorn`)
- [ ] Проверить что Яндекс Метрика регистрирует посещения (Метрика → Сводка)
- [ ] Создать цели в Метрике вручную:
  - `phone_call` — Позвонить
  - `map_click` — На карте
  - `instagram_click` — Instagram
  - `item_view` — Просмотр блюда
  - `category_select` — Выбор категории
  - `search_used` — Поиск
  - `search_result_click` — Клик на результат поиска
  - `language_switch` — Смена языка

---

## ВАЖНОЕ

### ~~Rate limiting на Search API~~ ✅ сделано
- `django-ratelimit` установлен, `/api/search/` ограничен 30 запросами в минуту с одного IP (403 при превышении)
- Файл: `menu/views.py`
- ⚠️ Нужно поставить `pip install -r requirements.txt` в рабочем venv — пакет добавлен в requirements, но не был установлен локально

### ~~Lazy loading изображений~~ ✅ сделано
- `loading="lazy"` добавлен на все `<img>` позиций в сетке меню и категорий
- Файлы: `menu.html`, `category.html`, `home.html`

### ~~Тесты~~ ✅ сделано
- Написаны тесты для моделей (`Item`, `Category`, `RoastedCoffee`), view (`Home`/`Menu`/`Category`/`Roasted`/`Beans`), поиска и rate limit
- Заодно найден и исправлен баг: `search_api`/`SearchView` не фильтровали `is_active` — архивные позиции могли всплывать в поиске
- Файл: `menu/tests/` (пакет вместо заглушки)

### ~~Убрать неиспользуемый файл~~ ✅ сделано
- `templates/includes/header.html` удалён (нигде не подключался)

---

## СРЕДНЕЕ

### Вынести JavaScript в отдельные файлы
- Создать `static/menu/js/search.js` — логика поиска (сейчас дублируется в home и menu)
- Создать `static/menu/js/modal.js` — логика модального окна
- Создать `static/menu/js/categories.js` — скролл и свайп категорий

### ~~Ограничить выборку на главной странице~~ ✅ уже сделано
- В `HomeView` уже стоит срез `[:6]` на `new_food`/`new_drinks`/`seasonal_food`/`seasonal_drinks`

### Schema.org разметка (SEO)
- Добавить JSON-LD блок в `base.html` с типом `CafeOrCoffeeShop`
- Добавить разметку `MenuItem` на страницу меню
- Даст расширенные сниппеты в Google

### Страница /beans/
- Принять решение: наполнить контентом ИЛИ скрыть из навигации
- Пока заглушка — вредит впечатлению

### Документация деплоя
- Создать файл `docs/deploy.md`
- Описать: Nginx конфиг, Gunicorn + systemd, переменные окружения, обновление

---

## НЕ ВАЖНОЕ (когда будет время)

### Динамический OpenGraph-образ
- Сейчас хардкод `favicon.png` — плохо выглядит при шаринге в мессенджерах
- Сделать отдельное OG-изображение 1200×630 и передавать через context

### Контент roasted.html перенести в базу данных
- Сейчас весь текст и ссылки на видео зашиты в HTML
- Создать модель `RoastedPage` или расширить `RoastedCoffee`
- Тогда можно редактировать без программиста через админку

### Пагинация результатов поиска
- `SearchView` отдаёт все совпадения подряд
- Добавить стандартный Django `Paginator`

### Вынести magic numbers в настройки
- `250ms` debounce, минимум `2` символа, максимум `10` результатов поиска
- Перенести в `config/settings.py`

---

## Идеи на будущее

- Добавить страницу "О нас" / история бренда
- Добавить раздел акций или сезонного меню
- Рассмотреть QR-код на столиках ведущий прямо в меню
- Добавить Google Analytics 4 параллельно с Метрикой (для международной аудитории)
